# Groq API key (required) — get one free at https://console.groq.com
# Used for: LLM text (llama-3.3-70b-versatile) and vision (llama-4-scout-17b-16e-instruct)
GROQ_API_KEY=gsk_your_key_here

# ChromaDB storage path (default shown — change if needed)
CHROMA_DB_PATH=./chroma_db
